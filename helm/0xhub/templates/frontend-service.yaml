apiVersion: v1
kind: Service
metadata:
  name: {{ include "hub.frontend.serviceName" . }}
  namespace: {{ include "hub.namespace" . }}
  labels:
    {{- include "hub.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hub.frontend.selectorLabels" . | nindent 4 }}

