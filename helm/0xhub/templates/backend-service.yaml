apiVersion: v1
kind: Service
metadata:
  name: {{ include "0xhub.backend.serviceName" . }}
  namespace: {{ include "0xhub.namespace" . }}
  labels:
    {{- include "0xhub.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "0xhub.backend.selectorLabels" . | nindent 4 }}

